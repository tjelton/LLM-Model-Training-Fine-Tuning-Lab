I want to make it clearer what the stages of progression are for students to help them when learning this content. I want to implement three main changes.

1. An introduction page which briefly explains what will be learnt in the lab and the learning outcomes (I will provide these below).
2. A flowchart that is now within each task, showing us where we are in the learning progression.
3. A final conclusion page reminding students what the learning outcomes were.

Similar to before, do not generate all these changes at once. Instead, I will instruct you on what to implement when.

The learning outcomes for this lesson are:
- To understand the key steps in LLM pre-training: tokenisation, embedding, attention, next-token prediction, loss calculation, and backpropagation.
- To understand how training differs from inference, and that there are different sampling methods for selecting the next token at inference time.
- To understand the difference between pre-training and fine-tuning.
- To critically evaluate the ethical concerns related to LLM pre-training and fine-tuning.

There should be a flowchart that is progressively updated as the lesson moves forward. This is to be placed in the summary section below each task header, but before the horizontal line. Before the flowchart, expand on the existing brief summary text to explain where we are up to in the lesson (remember to keep this brief). On the introduction and conclusion page, place the flowchart in its entirety. On the task pages, include the whole flowchart with the restrictions that if a part of the flowchart has not yet been addressed in a task, it is transparent (slightly greyed out). If a part of the flowchart was previously addressed, it should no longer be transparent. For a particular task and within the flowchart, indicate what part we are currently up to with some visual marker.

Here I describe the visualisation. The first part is a large rounded rectangle titled “Training”. Inside are two smaller rectangles that contain the words “Pre-training” and “Fine-tuning”, with an arrow from the “Pre-training” to the “Fine-tuning” rectangle. From the large rounded “Training” rectangle, have an arrow pointing towards another rectangle that says “Model”. Above this “Model” rectangle is a smaller rectangle called “Prompt”, with an arrow pointing towards “Model”. Out of the “Model” rectangle is an arrow pointing to a rectangle with the word “Output”. Next to this arrow, annotate the word “Inference”. Below the visualisation, include a line segment spanning the width of the visualisation, which underneath says “Ethical Considerations”.